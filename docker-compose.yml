version: '3.8'

services:
  client:
    build:
      context: ./client
    ports:
      - "3006:80"
    restart: always

  server:
    build:
      context: ./server
    ports:
      - "5006:5006"
    env_file:
      - ./server/.env
    restart: always
